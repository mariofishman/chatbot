# Example environment variables for the backend

# OpenAI-compatible API key (used later for LLM calls)
OPENAI_API_KEY=your-api-key-here

# Database URL (SQLite for local dev; upgraded later to Postgres if needed)
DATABASE_URL=sqlite:///./app.db

# Allowed CORS origins (comma-separated list for multiple origins)
CORS_ORIGINS=http://localhost:5173

# Default LLM model to use for assistant responses
LLM_MODEL=gpt-4o-mini

# Sampling temperature for LLM calls (0.0–1.0)
LLM_TEMPERATURE=0.0

# Enable or disable debug mode (affects logging and error detail)
DEBUG=true
